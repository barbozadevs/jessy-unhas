const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav-links');menuBtn?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const cursor=document.querySelector('.cursor-orb');window.addEventListener('pointermove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal,.service-card,.gallery-item,.step,.review-card').forEach((el,i)=>{el.style.transitionDelay=Math.min(i*.05,.3)+'s';observer.observe(el)});
let selected=5;const picker=document.getElementById('starPicker');function paintStars(n){picker?.querySelectorAll('button').forEach(b=>b.classList.toggle('active',Number(b.dataset.star)<=n))}paintStars(selected);picker?.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{selected=Number(b.dataset.star);paintStars(selected)}));
const KEY='jessy_unhas_reviews_v1';const load=()=>JSON.parse(localStorage.getItem(KEY)||'[]');const save=x=>localStorage.setItem(KEY,JSON.stringify(x));const list=document.getElementById('reviewList');const count=document.getElementById('reviewCount');const avg=document.getElementById('averageRating');
function renderReviews(){const reviews=load();count.textContent=reviews.length;if(!reviews.length){avg.textContent='5.0';list.innerHTML='<div class="empty-reviews">Ainda não há avaliações publicadas. Seja a primeira a contar como foi! ✨</div>';return}const average=reviews.reduce((a,r)=>a+r.rating,0)/reviews.length;avg.textContent=average.toFixed(1);list.innerHTML=reviews.slice().reverse().map(r=>`<article class="review-card"><div class="stars">${'★'.repeat(r.rating)}${'☆'.repeat(5-r.rating)}</div><p>“${escapeHtml(r.text)}”</p><small>${escapeHtml(r.name)} • ${r.date}</small></article>`).join('');document.querySelectorAll('.review-card').forEach(el=>observer.observe(el))}
function escapeHtml(s){return s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
document.getElementById('reviewForm')?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('reviewName').value.trim();const text=document.getElementById('reviewText').value.trim();if(!name||!text)return;const reviews=load();reviews.push({name,text,rating:selected,date:new Intl.DateTimeFormat('pt-BR').format(new Date())});save(reviews);e.target.reset();selected=5;paintStars(selected);renderReviews();document.getElementById('avaliacoes').scrollIntoView({behavior:'smooth'});});renderReviews();
